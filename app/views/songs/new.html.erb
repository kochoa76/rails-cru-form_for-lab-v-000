<h1> Song Form : </h1> 

<%= form_for(@song) do |f| %> 
<label> Song Name: </label> 
<%= f.text_field :name %> 

<%= hidden_field_tag :authenticity_token, form_authenticity_token %> 
<%= f.submit %> 
<% end %> 